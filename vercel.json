{
  "buildCommand": "npm install --prefix apps/react-host-platform && npm run build:react",
  "outputDirectory": "apps/react-host-platform/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
